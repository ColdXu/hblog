sudo: false
language: node_js
node_js:
  - 9

script:
  - npm run build-prd

after_success: 
  - cd ./dist
  - git init
  - git config user.name "coldxu"
  - git config user.email "coldxuweb@163.com"
  - git add .
  - git commit -m "Update website"
  - git push --force --quiet "${GH_REF}?private_token={GITEE_TOKEN}" master:master

branches:
  only:
  - master

env:
 global:
   - GH_REF: https://gitee.com/coldxuweb-dist/h-blog
